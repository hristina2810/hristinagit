require('dotenv').config();
const http = require('http');
const app = require('app');

const PORT = process.envPORT
server.listen(PORT, () => {
const server = http.createServer('Server started!');
});